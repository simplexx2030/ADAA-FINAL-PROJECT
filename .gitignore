# Secrets — never commit these (build spec Rule H)
.env
.env.local
*.key

# Cached Gemini replies (local only, never shared)
backend/.cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / frontend (from STEP 10 onward)
node_modules/
.next/
out/

# Old BidReady git history, kept locally as a backup only
.git-bidready-backup/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
